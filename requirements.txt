# ── Core ─────────────────────────────────────────────────────────────────────
numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.13,<2

# ── Survival / ML ────────────────────────────────────────────────────────────
scikit-learn>=1.5,<2
scikit-survival>=0.23,<1
lifelines>=0.29,<1
xgboost>=2.1,<3
optuna>=3.6,<5

# ── Anchor harness (Block AN — Lillelund position-cindex) ─────────────────────
# Author pins (torch==1.13 / pandas~1.5) are intentionally relaxed so they
# coexist with Domain lanes. Mirror: data/raw/anchor/position-cindex/requirements.txt
torch>=2.0,<3
survivaleval>=0.2.5
pycop>=0.0.13
statsmodels>=0.14,<1

# ── IO / download ────────────────────────────────────────────────────────────
requests>=2.31,<3
pyarrow>=19.0.1,<25
openpyxl>=3.1,<4
joblib>=1.4,<2

# ── Visualização (JMLR-oriented paper figures via SciencePlots) ───────────────
matplotlib>=3.9,<4
seaborn>=0.13,<1
SciencePlots>=2.1,<3
